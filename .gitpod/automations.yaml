tasks:
    runEchoCommand:
        name: Runs unit tests
        triggeredBy:
            - postDevcontainerStart
        command: echo '[MAIN] Container working_dir:' >> /workspaces/container.log && pwd >> /workspaces/container.log
